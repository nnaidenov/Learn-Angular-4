import { TitleCaseFormatPipePipe } from './title-case-format-pipe.pipe';

describe('TitleCaseFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaseFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
